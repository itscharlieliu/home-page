export interface Theme {
    primary: string;
    secondary: string;
    background: string;
    text: string;
}

const defaultTheme: Theme = {
    primary: "#fca311",
    secondary: "#14213d",
    background: "#000000",
    text: "#e5e5e5",
};

export default defaultTheme;
